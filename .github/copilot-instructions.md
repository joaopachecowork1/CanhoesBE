# Copilot Instructions

## Project Guidelines
- User prefers simple, ready-to-deliver solutions with good documentation for pull requests. The project uses .NET 10 and requires secure credential management - never expose passwords/API keys in appsettings.json. Use environment-specific configuration files (appsettings.Development.json) for local secrets and ensure .gitignore properly excludes all sensitive files.